# Zero-Token Sentinel (Cost Collapser) Requirements
# Main runtime: Node.js >= 18.0.0 (see package.json for primary dependencies)
#
# Python Requirements (for external integrations, LLM testing, API clients, and webhook handlers):
requests>=2.31.0
python-dotenv>=1.0.1
google-generativeai>=0.8.0
openai>=1.14.0
beautifulsoup4>=4.12.3
websockets>=12.0
pydantic>=2.6.0
