import type { Page } from 'playwright-core';
export interface NetworkCaptureEntry {
    kind: 'cdp';
    url: string;
    method: string;
    requestHeaders?: Record<string, string>;
    requestBodyKind?: string;
    requestBodyPreview?: string;
    requestBodyFullSize?: number;
    requestBodyTruncated?: boolean;
    responseStatus?: number;
    responseContentType?: string;
    responseHeaders?: Record<string, string>;
    responsePreview?: string;
    responseBodyFullSize?: number;
    responseBodyTruncated?: boolean;
    timestamp: number;
}
export declare class CloakNetworkCapture {
    private readonly limit;
    private readonly states;
    constructor(limit?: number);
    start(pattern: string, page: Page): void;
    read(page: Page): Promise<NetworkCaptureEntry[]>;
    stop(page: Page): void;
    private captureResponse;
    private bound;
}
