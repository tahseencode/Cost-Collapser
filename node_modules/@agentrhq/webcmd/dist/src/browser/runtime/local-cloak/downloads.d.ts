import type { Page } from 'playwright-core';
import type { BrowserDownloadWaitResult } from '../../../types.js';
export declare function waitForDownload(page: Page, pattern: string, timeoutMs: number): Promise<BrowserDownloadWaitResult>;
