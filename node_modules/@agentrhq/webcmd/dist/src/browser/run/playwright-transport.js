import { createRequire } from 'node:module';
import { BROWSER_RUN_PLAYWRIGHT_VERSION, BrowserRunError, } from './types.js';
function pageGuid(page) {
    const client = page;
    return client.guid ?? client._guid ?? '';
}
const coreBundle = createRequire(import.meta.url)('playwright-core/lib/coreBundle');
const { server } = coreBundle;
if (coreBundle.getPlaywrightVersion() !== BROWSER_RUN_PLAYWRIGHT_VERSION) {
    throw new Error(`Browser-run Playwright protocol requires ${BROWSER_RUN_PLAYWRIGHT_VERSION}.`);
}
const DENIED_METHODS = new Map([
    ['Browser', new Set([
            'close',
            'killForTests',
            'newBrowserCDPSession',
            'newContext',
            'newContextForReuse',
            'startServer',
            'stopServer',
        ])],
    ['BrowserContext', new Set(['close', 'newCDPSession'])],
    ['Page', new Set(['close'])],
    ['Playwright', new Set(['newRequest'])],
]);
const CLOSE_SESSION = 'end the session with `webcmd session close <session-id>`';
const NO_RAW_CDP = 'raw CDP is not exposed inside browser run; drive the page with the Playwright APIs on `page`';
const ONE_CONTEXT = 'a run is scoped to one context; start another with `webcmd session create`';
const API_REMEDIATIONS = {
    'Browser.close': CLOSE_SESSION,
    'Browser.newBrowserCDPSession': NO_RAW_CDP,
    'Browser.newContext': ONE_CONTEXT,
    'Browser.newContextForReuse': ONE_CONTEXT,
    'BrowserContext.close': CLOSE_SESSION,
    'BrowserContext.newCDPSession': NO_RAW_CDP,
    'Page.close': `the session owns its tabs; leave the tab open, or ${CLOSE_SESSION}`,
    'Playwright.newRequest': 'use `page.request` to make HTTP calls in the page\'s context',
};
export function unsupportedApiMessage(api) {
    const remediation = API_REMEDIATIONS[api];
    return remediation
        ? `${api} is unavailable in browser run; ${remediation}.`
        : `${api} is unavailable in browser run.`;
}
function implementation(object) {
    const client = object;
    const value = client._connection?.toImpl?.(object);
    if (!value) {
        throw new BrowserRunError('BROWSER_RUN_API_UNSUPPORTED', 'The supplied browser connection cannot be shared with browser run.');
    }
    return value;
}
function scopedContext(context, scope) {
    const pageListeners = new Map();
    const contextListeners = new Map();
    const related = (value, property) => {
        if ((typeof value !== 'object' || value === null) && typeof value !== 'function')
            return undefined;
        try {
            const candidate = Reflect.get(value, property, value);
            return typeof candidate === 'function' ? Reflect.apply(candidate, value, []) : candidate;
        }
        catch {
            return undefined;
        }
    };
    const requestPage = (request) => {
        const frame = related(request, 'frame') ?? related(request, '_frame');
        return related(frame, 'page') ?? related(frame, '_page');
    };
    const eventBelongsToScope = (event, args) => {
        let eventPage;
        if (event === 'request' || event === 'requestfailed') {
            eventPage = requestPage(args[0]);
        }
        else if (event === 'response') {
            eventPage = requestPage(related(args[0], 'request'));
        }
        else if (event === 'requestfinished') {
            eventPage = requestPage(related(args[0], 'request'));
        }
        else if (event === 'console') {
            eventPage = related(args[0], 'page');
        }
        else if (event === 'pageerror') {
            eventPage = args[1];
        }
        else if (event === 'recorderevent') {
            eventPage = related(args[0], 'page');
        }
        return eventPage !== undefined && scope.pages().includes(eventPage);
    };
    const addPageListener = (listener, once = false) => {
        let dispose = () => undefined;
        const registered = (page) => {
            if (once) {
                dispose();
                pageListeners.get(listener)?.delete(dispose);
            }
            listener(page);
        };
        dispose = scope.onPage(registered);
        const disposers = pageListeners.get(listener) ?? new Set();
        disposers.add(dispose);
        pageListeners.set(listener, disposers);
    };
    const removePageListener = (listener) => {
        for (const dispose of pageListeners.get(listener) ?? [])
            dispose();
        pageListeners.delete(listener);
    };
    const removeAllPageListeners = () => {
        for (const listener of pageListeners.keys())
            removePageListener(listener);
    };
    const addContextListener = (event, listener, once = false) => {
        let dispose = () => undefined;
        const registered = (...args) => {
            if (event !== 'close' && !eventBelongsToScope(event, args))
                return;
            if (once) {
                dispose();
                contextListeners.get(event)?.get(listener)?.delete(dispose);
            }
            listener(...args);
        };
        Reflect.apply(Reflect.get(context, 'on'), context, [event, registered]);
        dispose = () => Reflect.apply(Reflect.get(context, 'off'), context, [event, registered]);
        const byListener = contextListeners.get(event) ?? new Map();
        const disposers = byListener.get(listener) ?? new Set();
        disposers.add(dispose);
        byListener.set(listener, disposers);
        contextListeners.set(event, byListener);
    };
    const removeContextListener = (event, listener) => {
        const byListener = contextListeners.get(event);
        for (const dispose of byListener?.get(listener) ?? [])
            dispose();
        byListener?.delete(listener);
        if (byListener?.size === 0)
            contextListeners.delete(event);
    };
    const removeAllContextListeners = (event) => {
        const events = event === undefined ? [...contextListeners.keys()] : [event];
        for (const name of events) {
            for (const listener of contextListeners.get(name)?.keys() ?? []) {
                removeContextListener(name, listener);
            }
        }
    };
    const dialogManager = Reflect.get(context, 'dialogManager', context);
    const dialogHandlers = new Map();
    const scopedDialogManager = new Proxy(dialogManager, {
        get(target, property) {
            if (property === 'addDialogHandler') {
                return (handler) => {
                    const registered = (dialog) => {
                        const page = related(dialog, 'page');
                        return page !== undefined && scope.pages().includes(page)
                            ? handler(dialog)
                            : false;
                    };
                    dialogHandlers.set(handler, registered);
                    Reflect.apply(Reflect.get(target, property), target, [registered]);
                };
            }
            if (property === 'removeDialogHandler') {
                return (handler) => {
                    const registered = dialogHandlers.get(handler) ?? handler;
                    dialogHandlers.delete(handler);
                    Reflect.apply(Reflect.get(target, property), target, [registered]);
                };
            }
            const value = Reflect.get(target, property, target);
            return typeof value === 'function' ? value.bind(target) : value;
        },
    });
    let proxy;
    proxy = new Proxy(context, {
        get(target, property) {
            if (property === 'pages')
                return scope.pages;
            if (property === 'newPage')
                return scope.createPage;
            if (property === 'dialogManager')
                return scopedDialogManager;
            if (property === 'backgroundPages' || property === 'serviceWorkers')
                return () => [];
            if (property === 'on' || property === 'addListener') {
                return (event, listener) => {
                    if (event === 'page') {
                        addPageListener(listener);
                        return proxy;
                    }
                    addContextListener(event, listener);
                    return proxy;
                };
            }
            if (property === 'off' || property === 'removeListener') {
                return (event, listener) => {
                    if (event === 'page') {
                        removePageListener(listener);
                        return proxy;
                    }
                    removeContextListener(event, listener);
                    return proxy;
                };
            }
            if (property === 'once') {
                return (event, listener) => {
                    if (event === 'page') {
                        addPageListener(listener, true);
                        return proxy;
                    }
                    addContextListener(event, listener, true);
                    return proxy;
                };
            }
            if (property === 'removeAllListeners') {
                return (event) => {
                    if (event === undefined || event === 'page')
                        removeAllPageListeners();
                    if (event !== 'page')
                        removeAllContextListeners(event);
                    return proxy;
                };
            }
            if (property === 'waitForEvent') {
                return (event, optionsOrPredicate) => {
                    const options = typeof optionsOrPredicate === 'object' ? optionsOrPredicate : undefined;
                    const predicate = typeof optionsOrPredicate === 'function' ? optionsOrPredicate : options?.predicate;
                    return new Promise((resolve, reject) => {
                        let timer;
                        const removeListener = (listener) => {
                            if (event === 'page')
                                removePageListener(listener);
                            else
                                removeContextListener(event, listener);
                        };
                        const listener = async (value) => {
                            try {
                                if (predicate && !await predicate(value))
                                    return;
                                removeListener(listener);
                                if (timer)
                                    clearTimeout(timer);
                                resolve(value);
                            }
                            catch (error) {
                                removeListener(listener);
                                if (timer)
                                    clearTimeout(timer);
                                reject(error);
                            }
                        };
                        if (event === 'page')
                            addPageListener(listener);
                        else
                            addContextListener(event, listener);
                        if (options?.timeout) {
                            timer = setTimeout(() => {
                                removeListener(listener);
                                reject(new Error(`Timeout while waiting for event "${event}"`));
                            }, options.timeout);
                        }
                    });
                };
            }
            const value = Reflect.get(target, property, target);
            return typeof value === 'function' ? value.bind(target) : value;
        },
    });
    return proxy;
}
function scopedBrowser(browser, context) {
    const contextListeners = new WeakMap();
    let proxy;
    proxy = new Proxy(browser, {
        get(target, property) {
            if (property === '_defaultContext')
                return Reflect.get(target, property, target) ? context : undefined;
            if (property === 'contexts')
                return () => (Reflect.get(target, '_defaultContext', target) ? [] : [context]);
            if (property === 'on' || property === 'addListener') {
                return (event, listener) => {
                    let registered = listener;
                    if (event === 'context') {
                        registered = (candidate, ...args) => {
                            if (candidate === context)
                                listener(candidate, ...args);
                        };
                        contextListeners.set(listener, registered);
                    }
                    Reflect.apply(Reflect.get(target, property), target, [event, registered]);
                    return proxy;
                };
            }
            if (property === 'off' || property === 'removeListener') {
                return (event, listener) => {
                    const registered = contextListeners.get(listener) ?? listener;
                    Reflect.apply(Reflect.get(target, property), target, [event, registered]);
                    contextListeners.delete(listener);
                    return proxy;
                };
            }
            const value = Reflect.get(target, property, target);
            return typeof value === 'function' ? value.bind(target) : value;
        },
    });
    return proxy;
}
export class PlaywrightTransport {
    pageGuid;
    #connection;
    #root;
    #deliver;
    #registerPageImpl;
    #cancellation;
    #disposed = false;
    #browserWaitMs = 0;
    constructor(input, deliver) {
        if (!input.browser.contexts().includes(input.context)
            || !input.context.pages().includes(input.page)) {
            throw new BrowserRunError('BROWSER_RUN_API_UNSUPPORTED', 'The supplied page is outside the supplied browser context.');
        }
        const browser = implementation(input.browser);
        const context = scopedContext(implementation(input.context), {
            pages: () => input.pages().map(page => implementation(page)),
            createPage: async () => implementation(await input.createPage()),
            onPage: listener => input.onPage(page => listener(implementation(page))),
        });
        this.pageGuid = pageGuid(input.page);
        this.#registerPageImpl = page => { implementation(page); };
        for (const page of input.pages())
            this.registerPage(page);
        this.#deliver = deliver;
        this.#connection = new server.DispatcherConnection();
        this.#connection.onmessage = message => {
            if (!this.#disposed)
                this.#deliver(JSON.stringify(message));
        };
        this.#root = new server.RootDispatcher(this.#connection, async (scope, { sdkLanguage }) => new server.PlaywrightDispatcher(scope, server.createPlaywright({ sdkLanguage, isServer: true }), {
            denyLaunch: true,
            preLaunchedBrowser: scopedBrowser(browser, context),
            sharedBrowser: true,
        }));
    }
    send(message) {
        if (this.#disposed)
            return;
        let parsed;
        try {
            parsed = JSON.parse(message);
        }
        catch {
            throw new BrowserRunError('BROWSER_RUN_API_UNSUPPORTED', 'Browser-run sent an invalid Playwright protocol message.');
        }
        const dispatcher = typeof parsed.guid === 'string'
            ? this.#connection._dispatcherByGuid.get(parsed.guid)
            : undefined;
        const method = typeof parsed.method === 'string' ? parsed.method : '';
        if (dispatcher?._type === 'BrowserType'
            || dispatcher?._type === 'Android'
            || dispatcher?._type === 'Electron'
            || DENIED_METHODS.get(dispatcher?._type ?? '')?.has(method)) {
            this.#unsupported(parsed.id, `${dispatcher?._type}.${method}`);
            return;
        }
        const startedAt = Date.now();
        void this.#connection.dispatch(parsed).finally(() => {
            this.#browserWaitMs += Math.max(0, Date.now() - startedAt);
        });
    }
    get browserWaitMs() {
        return this.#browserWaitMs;
    }
    registerPage(page) {
        this.#registerPageImpl(page);
    }
    cancel(error) {
        if (this.#disposed)
            return Promise.resolve();
        this.#cancellation ??= this.#root.stopPendingOperations(error).catch(() => undefined);
        return this.#cancellation;
    }
    async dispose(error = new Error('Browser run ended')) {
        if (this.#disposed)
            return;
        await this.cancel(error);
        this.#disposed = true;
        this.#connection.onmessage = () => undefined;
        this.#root._dispose();
    }
    #unsupported(id, api) {
        queueMicrotask(() => {
            if (this.#disposed)
                return;
            this.#deliver(JSON.stringify({
                id,
                error: {
                    error: {
                        name: 'BrowserRunError',
                        message: `BROWSER_RUN_API_UNSUPPORTED: ${unsupportedApiMessage(api)}`,
                        stack: '',
                    },
                },
            }));
        });
    }
}
