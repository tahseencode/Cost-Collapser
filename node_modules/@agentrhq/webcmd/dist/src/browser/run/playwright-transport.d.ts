import type { Page } from 'playwright-core';
import type { BrowserRunSessionScope } from './runner.js';
export declare function unsupportedApiMessage(api: string): string;
export declare class PlaywrightTransport {
    #private;
    readonly pageGuid: string;
    constructor(input: BrowserRunSessionScope, deliver: (message: string) => void);
    send(message: string): void;
    get browserWaitMs(): number;
    registerPage(page: Page): void;
    cancel(error: Error): Promise<void>;
    dispose(error?: Error): Promise<void>;
}
