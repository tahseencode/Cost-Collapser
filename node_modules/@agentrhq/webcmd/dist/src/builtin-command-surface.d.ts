import type { Command } from 'commander';
export declare const LIST_COMMAND_DESCRIPTION = "List all available CLI commands";
export declare const LIST_FORMAT_DESCRIPTION: string;
export declare const COMPLETION_COMMAND_DESCRIPTION = "Output shell completion script";
export declare const COMPLETION_SHELL_DESCRIPTION = "Shell type: bash, zsh, or fish";
/** Configure built-in grammar shared by the local and hosted runtimes. */
export declare function configureListCommandSurface(command: Command): Command;
/** Configure completion grammar shared by the local and hosted runtimes. */
export declare function configureCompletionCommandSurface(command: Command): Command;
/** Configure plugin marketplace search grammar shared by local and hosted runtimes. */
export declare function configurePluginSearchSurface(command: Command): Command;
/** Configure plugin installation grammar shared by local and hosted runtimes. */
export declare function configurePluginInstallSurface(command: Command): Command;
/** Configure installed-plugin listing grammar shared by local and hosted runtimes. */
export declare function configurePluginListSurface(command: Command): Command;
/** Configure plugin uninstall grammar shared by local and hosted runtimes. */
export declare function configurePluginUninstallSurface(command: Command): Command;
/** Configure plugin update grammar shared by local and hosted runtimes. */
export declare function configurePluginUpdateSurface(command: Command): Command;
