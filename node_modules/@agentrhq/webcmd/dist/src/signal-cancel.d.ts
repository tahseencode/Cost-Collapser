export declare function installDaemonRunSignalCancellation({ process: proc, cancelRun, exit, }?: {
    process?: Pick<NodeJS.Process, 'once' | 'off'>;
    cancelRun?: (runId: string) => Promise<void>;
    exit?: (code: number) => void;
}): () => void;
