export type CommandOrigin = {
    kind: 'plugin';
    plugin: string;
} | {
    kind: 'override';
    plugin: string;
} | {
    kind: 'local';
} | {
    kind: 'builtin';
};
interface OriginPaths {
    pluginsDir?: string;
    userClisDir?: string;
}
/**
 * Classify where a registered command came from, by its `source` path.
 *
 * A command under ~/.webcmd/clis is reported as 'local' here. Whether it is
 * actually an override depends on a provenance record, which this module
 * deliberately does not read — callers combine the two.
 */
export declare function classifyCommandOrigin(command: {
    source?: string;
}, paths?: OriginPaths): CommandOrigin;
export declare function formatCommandOrigin(origin: CommandOrigin): string;
export {};
