import type { WebFetchResult } from './client.js';
export declare const webFetchCommand: import("../registry.js").CliCommand;
/** Run only the client-owned fetch command without loading the main CLI. */
export declare function runWebFetchCommand(argv: string[]): Promise<void>;
export declare function formatWebFetchMarkdown(result: WebFetchResult): string;
