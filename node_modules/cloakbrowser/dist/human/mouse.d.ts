/**
 * cloakbrowser-human — Human-like mouse movement and clicking.
 */
import { HumanConfig } from './config.js';
export interface RawMouse {
    move: (x: number, y: number) => Promise<void>;
    down: (options?: any) => Promise<void>;
    up: (options?: any) => Promise<void>;
    wheel: (deltaX: number, deltaY: number) => Promise<void>;
}
export interface RawKeyboard {
    down: (key: string) => Promise<void>;
    up: (key: string) => Promise<void>;
    type: (text: string) => Promise<void>;
    insertText: (text: string) => Promise<void>;
}
interface Point {
    x: number;
    y: number;
}
export declare function humanMove(raw: RawMouse, startX: number, startY: number, endX: number, endY: number, cfg: HumanConfig): Promise<void>;
export declare function clickTarget(box: {
    x: number;
    y: number;
    width: number;
    height: number;
}, isInput: boolean, cfg: HumanConfig): Point;
export declare function humanClick(raw: RawMouse, isInput: boolean, cfg: HumanConfig): Promise<void>;
export declare function humanIdle(raw: RawMouse, cx: number, cy: number, cfg: HumanConfig): Promise<void>;
export declare function humanIdle(raw: RawMouse, seconds: number, cx: number, cy: number, cfg: HumanConfig): Promise<void>;
export {};
//# sourceMappingURL=mouse.d.ts.map